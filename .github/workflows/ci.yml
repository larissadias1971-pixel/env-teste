name: Pipeline de Integracao Continua (CI)

#Gatilhos: Dispara o pipeline em Pushes e Pull Requests na branch principal
on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]
jobs:
  testes-e-validacao:
#Define o sistema operacional do runner(maquina virtual)
    runs-on: ubuntu-latest
  steps:
#Step 1: Baixa o codigo do repositorio para o rinner
    - name: Baixar Codigo-Fonte